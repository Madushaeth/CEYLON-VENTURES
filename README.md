cd ~/ceylon-ventures   # or wherever your local repo folder lives

# Rename the file
git mv ceylon-ventures-platform.html index.html

# Commit + push
git commit -m "Rename to index.html for GitHub Pages"
git push origin main
